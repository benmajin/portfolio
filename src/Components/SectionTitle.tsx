import React, { ReactNode } from "react"

const SectionTitle = ({ children }: { children: ReactNode }) => (
	<h2 className="text-4xl font-bold text-mainWhite">{children}</h2>
)

export default SectionTitle
